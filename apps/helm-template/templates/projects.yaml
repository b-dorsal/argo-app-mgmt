{{- range .Values.projects }}
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: {{ .name }}
  namespace: argocd
spec:
  description: {{ .name }} created by argo
  sourceRepos:
    - '*'
  destinations:
    - name: in-cluster
      namespace: argocd
  clusterResourceWhitelist:
    - group: '*'
      resource: '*'
  namespaceResourceBlacklist:
    - group: '*'
      resource: '*'
---
{{- end }}